[package]
name = "automatization"
version = "0.1.0"
edition = "2024"

[dependencies]

serialport = "4.3.0"


axum = { version = "0.7", features = ["ws"] }


tokio = { version = "1.0", features = ["full"] }

tower-http = { version = "0.6.8", features = ["cors"] }
tokio-stream = "0.1"
